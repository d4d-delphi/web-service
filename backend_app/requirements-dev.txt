# CI/dev install — includes runtime deps so `ruff check app` and `pytest` can
# import the app. (Render's deploy build uses requirements.txt, not this file.)
fastapi>=0.110
uvicorn[standard]>=0.27
pyyaml>=6.0
rdflib>=7.0
requests>=2.31
python-dotenv>=1.0

# dev / test
pytest==8.3.4
httpx==0.28.1
ruff==0.8.6
